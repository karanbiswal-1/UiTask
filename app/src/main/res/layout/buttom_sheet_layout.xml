<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent" android:background="@drawable/friends_rcv_bg">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Invite a Friend"
        android:layout_marginLeft="30dp"
        android:textSize="30dp"
        android:layout_marginTop="25dp"
        android:id="@+id/tv_inviteafriend_bst"
        android:textStyle="bold"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/search_et_bg"
        android:id="@+id/rl_et_search"
        android:layout_below="@id/tv_inviteafriend_bst">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_centerVertical="true"
            android:src="@drawable/ic_search"
            android:id="@+id/iv_search_et_bst"
            android:layout_marginLeft="5dp"/>

        <EditText
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_toRightOf="@id/iv_search_et_bst"
            android:layout_centerVertical="true"
            android:layout_marginLeft="5dp"
            android:background="#FFF"
            android:id="@+id/et_search_bmst"
            android:hint="Search a friend"/>



    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/rl_et_search"
        android:layout_marginLeft="20dp"
        android:nestedScrollingEnabled="true"
        android:layout_marginTop="20dp"
        android:id="@+id/rcv_invite_friends_bst"/>


</RelativeLayout>